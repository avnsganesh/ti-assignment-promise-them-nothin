// Multi-node harness: spawn 3 app nodes on different ports + a round-robin
// proxy on a single client-facing port, so one client hits different nodes
// between requests (mirrors RelayAPI's LB with no session affinity).
//
// SKELETON ONLY — not implemented yet.

console.error('scripts/cluster.js: not implemented yet (skeleton).');
process.exit(1);
